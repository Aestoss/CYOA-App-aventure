# Optional. Defaults shown.

# Port the server listens on.
PORT=3000

# Directory where the JSON database (db.json) is stored.
# On Railway, point this at a mounted volume (e.g. /data) so game data
# survives redeploys — see DEPLOY.md, étape 3bis.
DATA_DIR=./data
